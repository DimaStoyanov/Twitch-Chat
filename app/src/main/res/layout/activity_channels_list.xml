<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_chanels_list"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="?attr/colorButtonNormal"
    tools:context="irc.android_2016.ifmo.ru.irc.ChannelsListActivity">

    <TextView
        android:id="@+id/irc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textStyle="bold|normal"
        android:textSize="30sp"
        android:text="@string/irc_client"
        android:textAllCaps="true" />

    <LinearLayout
        android:id="@+id/ll"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:layout_width="match_parent"
        android:orientation="horizontal"
        android:layout_below="@id/irc"
        android:layout_height="wrap_content">

        <Button
            android:id="@+id/add_channel"
            android:layout_width="0dp"
            android:layout_weight="3"
            android:onClick="onAddChannelClick"
            android:background="@android:color/darker_gray"
            android:layout_height="match_parent"
            android:text="@string/add_channel" />

        <ImageButton
            android:id="@+id/settings"
            android:layout_width="0dp"
            android:layout_marginLeft="5dp"
            android:layout_height="wrap_content"
            android:onClick="onSettingsClick"
            android:src="@drawable/settings"
            android:layout_weight="1"
            android:background="@android:color/darker_gray" />
    </LinearLayout>

    <ScrollView
        android:id="@+id/channels_sv"
        android:layout_below="@id/ll"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:id="@+id/channels_ll"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </ScrollView>
</RelativeLayout>
